Build me Shipublic /dashboard working MVP

About Ship Public Project : 
.git commits, diffs, commit messages automatically turned into build in public posts.

A fully local automation tool that converts your Git commits and code changes into clean, engaging “build in public” updates.

It analyzes diffs, understands what changed and why, and generates ready-to-post content for platforms like LinkedIn or Twitter. No cloud. No API costs. Just your machine doing the thinking.

What it actually does
- Tracks your Git commits and diffs
- Understands changes like a developer, not a log parser
- Generates human-friendly updates (not boring changelogs)
- Automates your build-in-public workflow
- Runs completely offline using local LLMs (Ollama)
- The problem it solves

Most devs:

- ship quietly
- forget to post updates
- or write dull “fixed bug, added feature” posts 

Your tool turns that into:

clear, engaging progress updates… automatically

Value in one line

“Turn your commits into content without lifting a finger.”

Who it’s for
- Indie hackers building in public
- Developers who hate writing posts
- Busy Founders documenting progress
- Open-source shippers

Why it’s different
- No API costs (runs locally)
- Understands code diffs, not just commit messages
- Produces story-style updates, not logs
- Fits perfectly into a dev workflow (Git-based trigger)

Working Implementation : It should have

- Github OAuth
- github integration of project selection
- Fetch and Analyze my commit messages and file diffs via git
- calendar view render of my github calender and
- a seprate calendar for posts creation
- Old git commoits selection too
- Range of git selections
- Automatic post generation for Build in Public
- ollama integration
- To analyze code diffs and commit message use the ollama model 
qwen2.5-coder:32b → for structured summary 
qwen3.6:27b → final social post
- cron + Git hooks (have an auto sync toggle button - if enabled for certain project , once push it should trigger the flow and auto create build in public post)


Hybrid strategy (THIS is the real pro move)

Use two models in pipeline:

Coder model → understand diff (brain)
Chat model → polish post (post text (voice))

Prompt structure (important) (improvise as much as needed and improvise accordingly)

Feed it like this (don’t just dump diff blindly):

You are a developer writing a "build in public" update.

Summarize the following git diff:
- What changed
- Why it matters
- Keep it simple

Then convert it into:
- 1 short LinkedIn/Twitter style post
- Slightly engaging tone, not cringe

Git diff:
<PASTE DIFF>

Ollama tuning for your PC

Run with GPU assist:

OLLAMA_NUM_GPU=1 ollama run qwen2.5-coder:32b

OLLAMA_MAX_LOADED_MODELS=1

FLOW : git commit message + git diff → qwen2.5-coder:32b → structured summary → qwen3.6:27b → final social post

Important : It is an open source AGPL v3.0 locally run project for now. we will monetize and cloud it later with Cloud APIs

all latest version only (Important)

Frontend : NextJS + Typescript (both latest version only (Important))

No TailwindCSS (Important). use only CSS Modules

Don't use emojis or hiphen or underscore , - ,  _ anywhere in the frontend.

Use only Lucide icons (Important).

Backend : NestJS + Typescript + Fastify adapter + Prisma (all latest version only (Important))

DB : Postgresql

Optimization, Caching, Queues:  Redis + BullMQ

make no mistakes. It should work end to end. implement end to end. implement both the frontend and backend. improvise as much as needed. improvise accordingly. make it as an enterprise grade level application. Implement

Build our product PRODUCT_NAME_OS landing page. Analyze the project completely. I have attached the reference landing page for you to check. Keep it in single index.html file using only HTML,CSS,JS. I need the same structure and Ui/UX (Important) and layout of the attached landing page but change the contents according to our project

Follow the same pricing plans tiers and Auth flow from the landing pages

Don't use emojis or hiphen or underscore , - ,  _ anywhere in the frontend. Use only Lucide icons (Important).

Only use Plus Jakarta Sans for the Hero Landing slogan text section and hero texts.

Use inconsolata fonts everywhere for the whole application
Keep Sun and Moon lucide icons switch in navbar for dark and light theme switch

Have the graphics in the hero landing carousels.

Dark Mode as default : 
Black Background + White color for text content + Hero Color
Light Mode :
White Background + Black color for text content + Hero Color

Black Background : #1D1E21
Hero Color : #FF004F

1. move the landing page index.html to the nextJS application root . in NextJS root landing page this landing page should load

2. From Landing page if both the Demo button (navbar)+ Free Demo button (hero) is clicked it should load (remove current dummy alert)

# FREE DEMO 

# WITHOUT BACKEND ONLY STANDALONE FRONTEND FOR ONLY ONE SPECIFIC USERNAME AND PASSWORD

Analyze my application completely and create a free demo account with dummy demo data and mock API. It should look like using real application but only on read only. all the destructive actions of CRUD should be disabled.  But all the buttons should work with demo data. except read and view only. On clicking Free Demo from the root landing page button it should take me directly to the application with demo and mock data / mock API. I want to view everything same like real user/admin.

credentials to user and default auto fill in login screen

username : blessl.in
password : blessl.in

make no mistakes. It should work end to end. implement end to end. implement both the frontend and backend. improvise as much as needed. improvise accordingly. make it as an enterprise grade level application. Implement

1. In /dashboard/projects/PROJECT_NAME Apply button alignment / position is not correct fix
2. In left side bar 
2.1 move "+ Add Project" button to top under "My Projects". Add Project button should be fixed it should not in scroll like multiple projects
2.2 Add Project button in left side bar not working. fix
2.3 under "workspace" and "My Projects" add padding bottom 1rem
3. All the select dropdowns should use our own "custom select dropdown" matching our theme. it should be smooth, clean and minimalistic 
4. bottom line of every generated final post add a default signature line
"Auto build in public post crafted by @shipublic"
Have a option to edit the custom signature inside web app too
5. Isn't the contribution graph last 365 days ? or current year
Have same like Github
Last 365, current year and all the past years as a button and it should be dynamic same like Github


1. clean and minimalistic custom scroll bar
2. move github contribution section under recent post. let both take full width. the layout in /dashboard should be
4x dashboard statscards
recent posts
github activity
projects
3. Increase the Auto Sign in seconds to 8 seconds
4. Check box Ui wrong for signature fix it. make it clean and minimalistic. Bug screenshot attached. make a custom check box in red color too
5. Apply button is now fixed. but now Project section ui/ux got problem. its not in correct responsiveness. fix. screenshot attached. its on wrong layout and showing x axis scroll. fix

Analyze my project and check whether it analyzes file changes, files diff etc and pass that to LLMs ? enlighten me

Optimize the whole application end to end both the frontend and backend. Don't change any Ui/Ux or business logics. All HTML, CSS, JS. for React performance hooks -> useMemo, useCallback, React.memo, memoize, useRef, useTransition, useDeferredValue, List Virtualization, Image Optimization, Dynamic Imports, Script Optimization, Reduce bundle size, Cache aggressively -> HTTP caching, SWR / React Query, CDN caching, Avoid unnecessary state, Split components -> Smaller components re-render less and other optimizations

Completely optimize the backend also end to end like a Enterprise Grade level applicaion.

Don't change any business logics or Ui/Ux

Optimize the whole application end to end like a industry standard enterprise grade level application and give the performance improvements report at last. make no mistakes

write the final report in docs/optimization.MD file

1. We are working on Shipublic

I already have a canva like clone MVP in this workspace, called GreetOS for your reference. They both are in the same tech stack.

I need a option to generate a image for my FINAL Build in public post which shipublic generates at last passed through "OLLAMA_CHAT_MODEL=qwen3:32b"

It should be in a seperate module and it should have sidebar seperate module option too and it should be inside post generation module also i.e /dashboard/posts/POST_SLUG

1.2 In final post generation module. /dashboard/posts/POST_SLUG it should have an Generate build in public post image option also label named as "Generate Image" on click should edit the build in public text into the background image provided by the user in the "Gallery" sidebar module. it should also auto generate the image on whenever the build in public post is ready. It should also have the download button

1.3 In side bar "Gallery Module" The background image settings and the ratio settings can be manage in a new sidebar module "Gallery" which should show all the previous generated post and its options view, download etc, the settings for image ratios (have all default ratios options same like canva , we are concentrating mostly on LinkedIn, X.com (formerly twitter) and Instagram. It will be great if i can control/config the margin/borders of the default generated post where to where the build in public post should embed because i have a header and footer in the template.

make no mistakes. It should work end to end. implement end to end. implement both the frontend and backend. improvise as much as needed. improvise accordingly. make it as an enterprise grade level application. Implement

1.4 There should be a custom post edit feature also same like GreetOS so i can edit my final image post if i need any changes. Implement

3. Also add image and edit the existing demo data into the image Background in the demo data. I am attaching my demo bg image.check the ratio and act accordingly for demo mode. I have attached the default image in the chat workspace folder blessl.in.png do accordingly

make no mistakes. It should work end to end. implement end to end. implement both the frontend and backend. improvise as much as needed. improvise accordingly. make it as an enterprise grade level application. Implement

in shipublic landing page "GitHub OAuth" section card - the icon inside the card is not showing/rendering. fix

1. I am building a free opensource build in public post generation both text and image generation. It reads my git and file diffs, code and then pass it to opensource ollama qwen coder and then pass it to Qwen 3 for post generation. I have already build it now i want auto ai news generation post ai text and ai gen Image background or add in the post image.

Have as a seperate module in the sidebar in the Label . 
"AI News Gen"

The real pro move (what you should actually do)

APIs are not truly “free at scale”.

So i want a real free + scalable system, do this instead:

- Hybrid approach (recommended architecture)
- Step 1: Use RSS feeds (100% free, unlimited)

Pull from:

Google News RSS
TechCrunch RSS
Hacker News
Reddit (r/Artificial, r/MachineLearning) (I should be able to add the reddit r/GROUPNAME manually. have a module/config for that)

Have all the 4 options as a select checkbox for process and have a seperate boxes of fetched new same like our existing git commit boxes post generate button/boxes. So i can check the news and if interested i can generate the image and embed text on it.



Example:

https://news.google.com/rss/search?q=AI

- No API key
- No limits
- Fully free forever

- Step 2: Parse + deduplicate

Use:

Node.js → rss-parser
or
Python → feedparser

- Step 3: Summarize with your existing stack

we already have:

Ollama = Qwen coder + Qwen 3

So pipeline becomes:

RSS → Extract headlines → Fetch article → Summarize → Generate post

- Step 4: Generate images (FREE)

Here are your real free options:

- Local (best)
Stable Diffusion via ComfyUI

Final verdict for YOU
- Use ComfyUI as your main engine

Because:

You’re already building automation systems
You’re using Ollama + pipelines
You want zero-cost scalable generation
You only have 8GB VRAM (needs efficiency)

- ComfyUI fits like a glove 🧤

- What I would do (exact setup)
- Setup combo:
ComfyUI
SDXL / SD 1.5 models
ControlNet nodes
Image save + naming automation

-Pipeline:

AI news text → prompt generator → ComfyUI workflow → image → auto save → ready for review and manual post

What you can do fully free

With ComfyUI locally, you can:

Generate AI news images
Batch generate (100s of images)
Build pipelines (auto prompt → image → save)
Run offline (no internet needed after setup)
Integrate with your Git → AI → post system

- This is exactly what your project needs.

- Fully free, runs on your RTX 4060

- Step 5: Style it like a “build in public” post

Extra spicy idea (this could blow up your project)

Add a twist:

- “Explain AI news like a solo developer” (build your own good prompt)

One upgrade idea (this will level up your system)

Make your pipeline:

AI news → prompt generator → ComfyUI → image → auto watermark → post


- My theme color is only

#1D1E21 - Black Background color
#FFFFFF - white color for text
#FF004F - Hero Primary color for heading , title and important keyword marking in the post image

make no mistakes. It should work end to end. implement end to end. implement both the frontend and backend. improvise as much as needed. improvise accordingly on your own. make it as an enterprise grade level application. Implement

2. Implement a default image and default post for Demo Data too. Use the existing current background image (Black background with git commits in footer) as the post image + add demo ai text and add the image asset. I have added the asset to be embed in the existing image in the chat, add demo text too

1. Pipeline: RSS fetch → dedup → Ollama coder summary → Ollama chat polish → optional ComfyUI background → gallery render. Default sources (Google News AI, TechCrunch, HN, Reddit r/Artificial) lazy-seed per user.

Why this flow ? we no need "Ollama coder summary" for this ai news flow right ? fix

2. Don't add the asset image in the same post with current background image and add asset in the same image wrong. 
Generate image and keep it seperately as the second page in the gallery/post.

Flow

Ai Generated Build in public post -> Add the text in given bg image + Ai Image in second page
Ai news generated post ->   Add the text in given bg image + Ai Image in second page

3. In /dashboard/news
Sources cards Ui/Ux is not right. Fix. Ui Bug screenshot attached

1. Always make sure in the output The First page image is comfy Ui generated Ai image and second page image is Ai Post + BG image.

2. Implement download as pdf

3. Implement download as zip (seperate images grouped)

4. make sure in demo account all these features works. demo images will be downloaded in the user's system

make no mistakes. It should work end to end. implement end to end. implement both the frontend and backend. improvise as much as needed. improvise accordingly. make it as an enterprise grade level application. Implement

1. For mobile screens the app inside navigation is messed up. Implement a hambuger menu with side bar options / do whatever layout is best for user experience for mobile. make it enterprise grade level for mobile screens. 

2. In Landing page

2.1 Footer add in center 

vibe coded with (Hero color heart lucide icon) by blessl.in (Hero Red color, on click should goes to https://blessl.in)

2.2 x (formerly twitter ) icon is not showing fix
Link : https://x.com/blesslinjerish

2.3 Add Linkedin icon also in landing page footer bottom

https://www.linkedin.com/in/blesslinjerish

make no mistakes. It should work end to end. implement end to end. implement both the frontend and backend. improvise as much as needed. improvise accordingly. make it as an enterprise grade level application. Implement

